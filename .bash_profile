# Git Autocomplete Installation
if [ -f `brew --prefix`/etc/bash_completion ]; then
    . `brew --prefix`/etc/bash_completion
fi

# Hub Alias
alias git="hub"

# Stanford Cluster Aliases
alias myth="ssh brianlam@myth.stanford.edu"
alias corn="ssh brianlam@corn.stanford.edu"
